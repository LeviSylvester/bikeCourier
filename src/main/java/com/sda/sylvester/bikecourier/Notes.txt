Query<?> query = <-- accepta orice clasa existenta

runmain:-compilejartransformtomachinecode-putintarget-runfromthere

rename directory from curier to courier

git add -p te intreaba ce linii de cod vrei sa adaugi

core.autocv crlf search command on google if you want to setup git with crlf line endings

in descriere la commit scriem ce s-a schimbat in cod

pana la primul commit git branch nu arata branch-ul

git log iti da istoricul

git branch -a arata branch-urile

git branch -D functionality de ex.<-- sterge branch-ul functionality

git checkout -b functionality creeaza un branch nou

git branch --set-upstream-to=origin/<branch> functionality ca sa poti sa adaugi din nou pe github

git fetch uita-te daca s-au facut schimbari in cod

git checkout -b origin functionality

git checkout -b <local> <remote>/<br se pune nume fara semne ca sa aduci coddul acela de pe server

git log --oneline arata codul branch-urilor

git checkout <codul acela> ma intorc in istoric

git clone <link> sau file/new project/from version control

ne ajuta librarii ex. apache commons - string utils; apache commons - commons collections

when you map xy coordinates with autoit tool, add 13 to x and 10 to y

